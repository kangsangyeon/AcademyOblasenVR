using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HandBool : MonoBehaviour
{
    [SerializeField] Hand hand;
    void Start()
    {
        //var selectSkillPhase = GameObject.FindObjectOfType<SelectSkillPhase>();
        //selectSkillPhase.OnSelectElementType.AddListener();
        //selectSkillPhase.OnClickBackButton.AddListener();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void GetElementType()
    {

    }
}
